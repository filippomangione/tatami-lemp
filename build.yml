version: "3.7"

services:
  php:
    build:
      context: ./
      dockerfile: Dockerfile
    image: ${APP_NAME}-php
  nginx:
    build:
      context: ./
      dockerfile: Dockerfile-nginx
    image: ${APP_NAME}-nginx
